{
  "name": "bata-mysql-to-supabase-sync",
  "version": "1.0.0",
  "description": "Incremental sync of dispatch/receiving from MySQL (reporting) into Supabase, plus a LAN-only live API",
  "main": "sync.js",
  "scripts": {
    "start": "node sync.js",
    "start:api": "node api-server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.10.0",
    "node-cron": "^3.0.3"
  }
}
